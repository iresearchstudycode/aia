# TODO

## Code Hygiene

- [x] Replace `notice.style.cssText` in `addContextTrimNotice()` with CSS class `.context-trim-notice`
- [x] Remove commented-out debug log in `speakText()` (`speech.js`)
- [x] Remove informational `console.log` calls in `chat.js` (`clearChat`, `saveChat`, `handleOpenFile`)
- [x] `__pycache__/`, `*.pyc`, `*.pyo`, `.pytest_cache/` added to `.gitignore`
- [x] `httpx` → `httpx2` in `auth/requirements-test.txt`
- [x] `white-space: pre-wrap` on `.user-message .message-content p` — Shift+Enter newlines now preserved in the displayed chat bubble
- [x] Guard `autoTTSBtn` localStorage restore behind `'speechSynthesis' in window` — prevents the button appearing green/active on browsers without Web Speech API TTS

## Security Improvements

- [x] Add file size validation in `handleOpenFile()` — 5 MB cap before `FileReader.readAsText()`
- [x] Pin vendored libraries to SRI hashes in `index.html` (`marked.min.js`, `dompurify.min.js`)
- [x] TOTP authentication — session-cookie auth via FastAPI + pyotp, gated by Nginx auth_request
- [x] TOTP replay protection — (username, code) pairs tracked in memory for 90 s; replayed codes rejected even within the valid TOTP window
- [x] Logout CSRF protection — HMAC-derived `vpal_csrf` cookie (non-HttpOnly); injected into logout form by `main.js`; verified server-side via double-submit cookie pattern
- [x] Nginx rate limiting on `/auth/login` — 20 req/min per IP, burst 5, returns 429; complements the in-process per-username lockout
- [x] Graceful session-expiry handling in `api.js` — `redirect: 'manual'` detects the nginx 302→login redirect; shows "Session expired — sign in again" with a link rather than silently failing
- [x] Copy/Speak buttons hidden until AI response fully and successfully completes — not shown on error, abort, or partial content
- [x] `microphone=(self)` added to nginx `Permissions-Policy` header
- [x] Chainguard base image digests pinned in `auth/Dockerfile` — both `FROM` lines use `@sha256:...` for reproducible, supply-chain-safe builds
- [x] Non-HttpOnly `vpal_user` cookie set at login for the JS profile widget; carries only the display username (no authentication capability); cleared on logout
- [x] Cloud-hardening plan produced — `docs/cloud-hardening-plan.md` (analysis only, no code/config changes); 15 prioritized findings, P0 blockers B/C/D/E/F/N to resolve before any internet exposure
- [x] Finding E (P0) — `deploy/nginx/nginx.conf` parametrized via `deploy/nginx/nginx.conf.template` + `deploy/nginx/render-nginx-conf.sh` (POSIX `sh` + `envsubst` with an explicit allowlist so nginx's own `$host`/`$binary_remote_addr` stay literal). 7 deployment placeholders: `AUTH_UPSTREAM`, `VOICEBOX_PROXY_UPSTREAM`, `DOC_EXTRACT_UPSTREAM`, `OLLAMA_UPSTREAM`, `SERVER_NAME`, `SSL_CERT_PATH`, `SSL_KEY_PATH` — defaults reproduce the committed file byte-for-byte (round-trip verified). `nginx-config-check` CI job extended with a drift guard (committed `nginx.conf` == default render) and a cloud-value render sanity check (no `${...}` left unsubstituted). `docker-compose` still mounts the committed local-default `nginx.conf`; **deferred follow-up: docker-compose init-container render for a fully template-driven runtime** (plus Findings D/F template values — `real_ip`, `TLS_MODE`, HSTS flags — deliberately out of scope here).
- [x] Version bumped to 1.16.0
- [x] Security hardening **paused** after Finding E — status & handoff captured in `docs/hardening-status.md` (session summary, completed work, remaining P0/P1/P2 tasks in dependency order with per-task sub-agent model recommendations, resume instructions). Detailed spec remains `docs/cloud-hardening-plan.md`.
- [x] All six SRI-pinned vendored assets (`marked.min.js`, `dompurify.min.js`, `katex.min.js`, `katex-auto-render.min.js`, `diff-match-patch.js`, `css/katex.min.css`) marked `binary` in `.gitattributes` and their `index.html` `integrity=` hashes recomputed from the committed LF bytes — fixes SRI mismatch that killed the app on a clean (`core.autocrlf=false`) Linux/Docker checkout. New `npm run check:sri` guard (`scripts/check-sri.mjs`), enforced in the `frontend-lint` CI job.

## Testing

- [x] Pytest test suite for `auth/main.py` — 65 tests covering all route handlers, brute-force lockout, TOTP replay protection, CSRF token derivation, session validation, `vpal_user` cookie lifecycle, `/auth/me` endpoint, and security edge cases (`auth/tests/test_main.py`)

## Build / Operational

- [x] `.dockerignore` for auth build context — excludes `tests/`, `__pycache__/`, `.env`, `*.pyc`, and editor artefacts from the Docker build context (`auth/.dockerignore`)
- [x] GitHub Actions CI workflow (`.github/workflows/ci.yml`) — runs `black --check`, `flake8`, and `pytest -v` on every push and PR to `master`
- [x] ESLint for frontend JS — `eslint.config.js` with browser globals and cross-module names; `npm run lint:js` targets `src/aia/scripts/*.js` (excludes vendored `*.min.js`); zero violations on clean run
- [x] Frontend lint job added to CI — `frontend-lint` job in `.github/workflows/ci.yml` runs `npm ci` + `npm run lint:js` on every push and PR to `master`
- [x] Chainguard digest-check scheduled workflow — `.github/workflows/chainguard-digest-check.yml` pulls both `cgr.dev/chainguard/python` images monthly (1st of month, 09:00 UTC) and fails with remediation instructions if either digest differs from the pins in `auth/Dockerfile`; also supports `workflow_dispatch` for on-demand runs
- [x] nginx `auth_login` rate-limit zone renamed to `auth_api` — reflects that the zone covers both `/auth/login` (burst=5) and `/auth/me` (burst=10), not login alone
- [x] Version bumped to 1.4.0
- [x] ChatGPT-style input area redesign — flat flex row replaced with a rounded `.chat-input-container`; toolbar row holds mic/speaker icon buttons, Auto-speak toggle, char counter, and a circular send button (↑ arrow, dark when active, grey when empty) and stop-streaming button (■); `setStreamingUI` re-evaluates textarea content when streaming ends so send button state is always accurate; `aria-label` added to both icon-only buttons
- [x] HTMLHint for `src/aia/index.html` — `htmlhint` devDependency added; `.htmlhintrc` config; `npm run lint:html` targets `index.html`; `lint:html` step added to the `frontend-lint` CI job
- [x] Nginx config syntax check added to CI — `nginx-config-check` job generates dummy certs, mounts `nginx.conf` into `nginx:1-alpine`, and runs `nginx -t` on every push and PR to `master`
- [x] Version bumped to 1.5.0
- [x] Version bumped to 1.5.1

## Features / UX

- [x] Make speech recognition language configurable — `SPEECH_RECOGNITION_LANG` in `config.js`
- [x] Character counter on input field — appears when ≤ 500 chars remaining, warns at 200, red at 50
- [x] Persist `autoTTS` preference to `localStorage` across sessions
- [x] Auto-speak `<label>` checkbox replaced with a 🔊 icon toggle button (`autoTTSBtn`) — consistent toolbar style; `aria-pressed` tracks on/off state; `tts-on` CSS class shows green active state; localStorage key unchanged so existing preferences are preserved
- [x] Profile menu widget in chat header — 👤 icon + logged-in username (title-cased from `vpal_user` cookie, with `/auth/me` fallback for existing sessions); dropdown contains Save, Open, Clear, Close, Sign out; replaces standalone header buttons
- [x] `/auth/me` GET endpoint in auth service — validates session cookie, returns `{"username": "..."}` as JSON; used by profile widget when `vpal_user` cookie is absent
- [x] Persona dropdown — ▾ button next to "🤖 AI Assistant" opens a fixed-position panel with the persona select; selected persona name displayed as a subtitle beneath the heading; renamed from "System Prompt" to "Persona"; replaces the always-visible System Prompt bar
- [x] Persona toggle locked state — `personaToggleBtn` gains `.locked` class and updated tooltip when a conversation is active; panel shows an inline notice; `updateSystemPromptState()` owns all persona-widget visual state
- [x] Profile dropdown and persona panel close on window resize — prevents stale `position:fixed` coordinates after viewport dimensions change
- [x] User input upgraded to auto-growing `<textarea>` — expands up to 200 px as content grows; Shift+Enter inserts a newline, Enter sends; placeholder text communicates the Shift+Enter convention; voice dictation dispatches `input` events so the textarea resizes for speech too
- [x] `aria-pressed` on `micBtn` — updated in all five code paths that change recording state across `speech.js` and `chat.js`; initial `aria-pressed="false"` set in HTML
- [x] `saveChat` success toast — brief "Chat saved" notification shown after download via `showToast()` in `utils.js` (reuses existing `.toast` CSS class)
- [x] Save filename format changed to `YYYYMMDD-HHMMss-vpal-<Topic>.json` — `_chatTopic()` helper in `saveChat()` strips punctuation, filters ~45 English stopwords, takes the first 2–3 meaningful words from the opening user message, title-cases them, and joins with hyphens; falls back to `'Chat'` when no qualifying words are found
- [x] Copy and Speak action buttons redesigned to ChatGPT-style icon-only toggles — SVG icons (`COPY_ICON`, `CHECK_ICON`, `SPEAK_ICON`, `STOP_ICON`) defined as top-level constants in `chat.js`; `SPEAK_ICON`/`STOP_ICON` shared with `speech.js` for per-message button state updates; `.action-btn` base class with `.copy-btn`/`.speak-btn` secondary classes preserve `api.js` querySelector compatibility; all four icons declared in `eslint.config.js` crossModuleGlobals
- [x] `type="button"` added to all non-submit buttons in `index.html` — prevents implicit `type="submit"` default on toolbar, send, stop, and dropdown menu buttons
- [x] `:focus-visible` outlines added to all interactive controls — `.action-btn`, `.voice-btn`, `.toolbar-icon-btn`, `.send-btn`, `.stop-streaming-btn`, `.persona-toggle-btn`, `.profile-trigger`, and `.profile-dropdown button` now show keyboard focus indicators (white outline on dark header, purple on light backgrounds)
- [x] Version bumped to 1.5.2
- [x] `#chatMessages` annotated with `role="log"`, `aria-label="Chat messages"`, and explicit `aria-live="polite"` — screen readers now identify the chat transcript and announce new messages as they arrive
- [x] `#userInput` textarea given `aria-label="Message"` — replaces placeholder-only labelling, which is not a substitute for an accessible label
- [x] HTMLHint `tag-pair` rule enabled in `.htmlhintrc` — passes cleanly (SVG self-closing elements do not trigger false positives); now catches unclosed HTML structural tags
- [x] CI `nginx-config-check` job pinned from `nginx:1-alpine` to `nginx:1.27-alpine` — eliminates silent breakage risk from a future 1.x minor-version config-syntax change
- [x] `showToast` timer moved from DOM node property (`toast._timer`) to module-level variable (`_toastTimer`) in `utils.js`
- [x] Version bumped to 1.5.3
- [x] `role="menu"` and `role="menuitem"` removed from profile dropdown — the ARIA menu role requires arrow-key navigation and forbids intermediate `<form>` wrappers; plain buttons are semantically correct and keyboard-navigable via Tab
- [x] `#systemPromptSelect` given `aria-labelledby="personaPanelHeader"` — programmatically associates the "Persona" heading with the select element; `id="personaPanelHeader"` added to the heading div
- [x] Persona panel focus management — `openPersonaPanel()` moves focus to the select when it is not disabled; `closePersonaPanel()` returns focus to `personaToggleBtn` when the active element is inside the panel at close time
- [x] CLAUDE.md CI section updated from `nginx:1-alpine` to `nginx:1.27-alpine` to match the actual CI job
- [x] `.github/dependabot.yml` added — monthly Dependabot updates for both `npm` devDependencies and GitHub Actions workflow steps
- [x] Version bumped to 1.5.4
- [x] Gemma 4 thinking mode enabled in `api.js` — assistant prefill with `<|think|>` token primes reasoning; Ollama sampling options set (`temperature: 1.0`, `top_p: 0.95`, `top_k: 64`); "Thinking…" placeholder shown while the model reasons; `<|/think|>` boundary strips internal reasoning from displayed response, conversation history, and copy/speak button data; stop-during-thinking rolls back the user message cleanly
- [x] Version bumped to 1.5.5
- [x] Streaming thinking UI with collapsible block — reasoning content displayed live in an open `<details>` block during the thinking phase; on transition to the answer the block is locked (DOM stable, no `<details>` destruction during answer streaming); on stream completion the block collapses automatically and summary text changes from "Thinking…" to "Thinking"; user can re-expand at any time; thinking content is never saved to `conversationHistory` or exported to saved chat files
- [x] Version bumped to 1.5.6
- [x] Thinking mode dual-buffer refactor — `thinkingBuffer` captures native Ollama `message.thinking` tokens; `fullResponse` captures `message.content`; inline `<|/think|>` token boundary in `message.content` also supported; correct mode detected per-token so both Ollama native thinking and prefill-token approaches work without configuration
- [x] Markdown rendering applied to thinking content — `DOMPurify.sanitize(marked.parse(...))` replaces `escapeHtml()` for all three thinking render sites (live thinking phase, transition, final rebuild); `white-space: pre-wrap` removed from `.thinking-content` CSS
- [x] `.thinking-content` descendant CSS specificity fixed — added overrides so `p`, `li`, `h1`–`h6`, `strong`, `em`, `blockquote` inside `.thinking-content` inherit the muted `#718096` colour rather than being overridden by `.message-content` descendant rules; heading `font-size` reset to `inherit` to preserve the compact 0.8rem scale
- [x] "Stop speaking" button (🔇) repositioned to the right of "Auto-speak" (🔊) in the toolbar — new order: 🎤 mic → 🔊 auto-TTS → 🔇 stop speaking
- [x] Version bumped to 1.5.7
- [x] Abort mid-answer now preserves thinking block — the abort handler derives `abortThinking` via `splitThinkingContent` and renders a collapsed `<details>` block alongside the partial answer and `[response stopped]` notice (previously the thinking block was silently discarded)
- [x] `splitThinkingContent(thinkingBuffer, fullResponse)` extracted as a pure utility in `utils.js` — replaces the three copies of the inline split logic in `api.js`; `_THINK_END` constant lives alongside it; Node.js compat export enables Jest testing
- [x] Jest unit tests added — `tests/js/thinking.test.js` covers `splitThinkingContent` across native mode, inline token mode, and abort scenarios (14 tests); `jest.config.js` added at project root; `npm run test:js` script added to `package.json`; `Test (jest)` step added to the `frontend-lint` CI job
- [x] `aria-label="Toggle AI reasoning"` added to all `<summary>` elements in the thinking block — thinking phase, transition, final rebuild, and abort rebuild; screen readers now get a consistent description regardless of the summary text state ("Thinking…" vs "Thinking")
- [x] Initial response bubble uses Unicode ellipsis `Thinking…` (U+2026) — matches the streaming thinking block summary; previously used ASCII `Thinking...`
- [x] `test_main.py` module docstring updated to list `/auth/me` in the route handlers coverage summary
- [x] `.thinking-content` max-height increased from 280px to 400px — gives more room for long reasoning chains before the scrollbar kicks in
- [x] Version bumped to 1.5.8
- [x] Image attachment for multimodal prompts — 📎 toolbar button opens a file picker; selected image is read via FileReader.readAsDataURL, validated for type (image/*) and size (≤ 10 MB), shown as a thumbnail preview strip above the textarea (with ✕ remove), and sent in the Ollama API `images` field as raw base64; the full data URL is stored in conversationHistory for in-session thumbnail display; imageBase64/imageDataUrl are stripped from saved JSON files (hasImage flag preserved so loaded history shows a placeholder); send button enabled with image even when textarea is empty; messages with only an image and no text are supported
- [x] clearImagePreview() defined as a top-level function in main.js so sendMessage/sendMessageAndContinueListening in chat.js can call it across script files
- [x] Version bumped to 1.6.0
- [x] Image preview strip startup visibility fixed — CSS `.image-preview-strip` default changed from `display:flex` to `display:none`; redundant inline `style="display:none"` and empty `src=""` removed from HTML
- [x] Nginx `client_max_body_size 20m` override added to `/ollama/api/chat` location — global 1 MB cap blocked multimodal payloads (base64 image ≈ 1.33× raw file size)
- [x] Nginx CSP `img-src 'self' data:` added — allows FileReader-produced data: URLs to render in image preview and chat bubbles
- [x] Ollama error detail surfaced — non-200 responses now read the JSON body and append `body.error` to the thrown message for easier diagnosis
- [x] Vision routing: image requests use `VISION_MODEL_NAME` (`gemma3:4b`, confirmed vision-capable) with `stream:false` and no sampling options; text requests keep `MODEL_NAME` (`gemma4:e4b`) with streaming, thinking prefill, and sampling options — `gemma4:e4b` has no vision encoder in its GGUF
- [x] Version bumped to 1.6.1
- [x] Multi-turn vision conversations — `detectVisionContext(imageBase64, history)` in `utils.js` checks both the current message and all history entries; `isVision=true` persists across follow-up text turns, keeping the conversation on `VISION_MODEL_NAME` (gemma3:4b); thinking prefill and sampling options suppressed for all vision turns; follow-up turns use `stream:true` for live token display
- [x] Resize notification — `showToast()` fires in `main.js` after canvas downscale with scaled dimensions; `calcResizeDims` return compared against original to detect whether resize occurred
- [x] Memory optimisation — `pendingImageDataUrl` now stores the resized data URL instead of the original; in-chat thumbnail and history both use the 1024px JPEG, eliminating up to 10 MB of in-memory data URL for large originals
- [x] `calcResizeDims(naturalWidth, naturalHeight, maxDim)` and `detectVisionContext(imageBase64, history)` extracted as pure utilities in `utils.js` — Node.js compat export added; called from `main.js` and `api.js` respectively
- [x] Jest tests for vision utilities — `tests/js/vision.test.js` covers `calcResizeDims` (landscape, portrait, square, small, boundary) and `detectVisionContext` (current image, history image, multi-turn, null/undefined guards) — 17 tests
- [x] num_ctx hint for multi-turn vision follow-ups — `requestBody.options = { num_ctx: 8192 }` sent when `isVision && !hasCurrentImage` to prevent silent context truncation
- [x] File size cap lowered — `MAX_IMAGE_SIZE_BYTES` reduced from 10 MB to 5 MB; alert message references the constant dynamically
- [x] CLAUDE.md updated — `VISION_MODEL_NAME` added to Key Configuration; Security Invariants nginx note updated to reflect 1 m global + 20 m per-location cap
- [x] README.md updated — overview and prerequisites mention image attachment and `gemma3:4b` vision model
- [x] 404 error message uses actual model name (`requestBody.model`) rather than hardcoded `MODEL_NAME`
- [x] Version bumped to 1.6.2
- [x] `_buildRequestBody(imageBase64, history, systemPrompt, modelName, visionModelName)` extracted from `streamOllamaResponse` in `api.js` — pure function with no DOM or global state; returns `{ requestBody, isVision, hasCurrentImage }`; Node.js compat export added; `streamOllamaResponse` destructures `{ requestBody, hasCurrentImage }` and passes globals as arguments
- [x] Integration tests for request routing — `tests/js/routing.test.js` covers model selection, stream mode, thinking prefill, options, messages array construction, and returned flags across text-only / initial vision / follow-up vision scenarios (17 tests; total suite now 50 tests across 3 files)
- [x] Version bumped to 1.6.3
- [x] Thinking mode control — 💡 toolbar button toggles thinking ON/OFF (default OFF); when ON a depth selector appears inline offering Low / Medium / High; `currentThinkingMode` global (`'off'|'low'|'medium'|'high'`) wired in `main.js`; depth defaults to Medium when first enabled
- [x] `_buildRequestBody` updated: `thinkingMode` parameter (default `'off'`) controls whether the `<|think|>` prefill is injected and whether `thinking_budget` is added to options (Low=1024, Medium=4096, High=no cap); sampling options applied regardless of thinking mode; vision requests always ignore thinking mode
- [x] `currentThinkingMode` added to `config.js` and `eslint.config.js` globals
- [x] Jest routing tests updated to pass `thinkingMode` explicitly; 8 new tests cover all four modes (off/low/medium/high) for prefill, options, and budget values — total suite now 58 tests
- [x] Version bumped to 1.7.0
- [x] Bug fix: thinking mode OFF did not suppress reasoning — `gemma4:e4b` reasons by default; `think: false` must be sent explicitly in the request body; `requestBody.think = thinkingEnabled` added in `_buildRequestBody`; 6 new routing tests assert `think` field value across all modes and vision cases — total suite now 64 tests
- [x] Version bumped to 1.7.1
- [x] Bug fix: with thinking OFF, an empty `<details class="thinking-block">` was rendered during streaming because the `!inAnswerPhase` branch always inserted the block regardless of whether `currentThinking` was non-empty; fixed by only emitting the block when `currentThinking` is truthy — answer now renders directly into `.answer-content` with no thinking widget visible
- [x] Version bumped to 1.7.2
- [x] Bug fix: answer rendered inside thinking block when thinking mode is OFF — root cause: `splitThinkingContent('', answer)` returns `{ thinking: answer, answer: '' }` (hits the "inline mode, still thinking" branch); fixed by computing `thinkingActive` once after `_buildRequestBody` and bypassing `splitThinkingContent` in all three call sites (live stream loop, final render, abort handler) when false; answer now streams directly into `contentDiv` with no thinking block
- [x] Version bumped to 1.7.3
- [x] ChatGPT-style redesign with sky blue + dark navy palette — page background dark navy (#0f172a); header solid dark navy with sky-blue chat-bubble logo; user messages right-aligned rounded pill (#dbeafe sky-blue bubble, no left border); AI messages clean card (subtle border, no left border); send button sky blue (#0ea5e9); all toolbar and dropdown emoji replaced with inline ChatGPT-style SVG line icons (mic, volume, mute, paperclip, lightbulb, user, chevron, download, folder, trash, X, logout); thinking-mode active state sky blue (not purple); speak-button speaking state sky blue; action-button hover sky blue; all indigo/purple accent (#667eea, #764ba2) replaced throughout
- [x] Version bumped to 1.8.0
- [x] Page background lightened to light grey (#f1f5f9 / Tailwind slate-100) — dark navy was too harsh for extended reading sessions
- [x] Vision history routing fix (P1) — `detectVisionContext` now checks `m.hasImage === true` in addition to `m.imageBase64`; loaded chats where base64 was stripped on save now correctly route follow-up questions to `VISION_MODEL_NAME` (gemma3:4b) instead of falling back to `MODEL_NAME`; 3 new Jest tests added (total suite 67 tests)
- [x] Vision abort desync fix (P2) — aborting an initial vision request no longer pops the user's history entry while the user bubble remains in the DOM; `conversationHistory.pop()` skipped when `error.name === 'AbortError' && hasCurrentImage`
- [x] Windows ESLint glob fix (P3) — `lint:js` script changed from `eslint 'src/aia/scripts/*.js'` (shell glob, broken on Windows) to `eslint src/aia/scripts` (directory); `*.min.js` exclusion moved to a global `{ ignores: ['**/*.min.js'] }` block in `eslint.config.js` (standalone block without `files` applies globally in ESLint v9 flat config)
- [x] Vision placeholder UX improvement (P4) — saved-chat image placeholder changed from plain text to an inline SVG camera/landscape icon + "Image not available in saved file" label; `.image-placeholder` flex layout and `.image-placeholder svg` sizing added to CSS
- [x] Chevron rotation animation (P5) — persona toggle chevron and profile trigger chevron now rotate 180° when their panel is open via CSS `transition: transform 0.2s ease` and `[aria-expanded="true"] svg { transform: rotate(180deg) }`
- [x] Version bumped to 1.9.0
- [x] Bug fix: `clearChat()` and `handleOpenFile()` now call `clearImagePreview()` — pending image attachment no longer silently persists into a cleared or newly loaded conversation
- [x] Thinking mode preference persisted to `localStorage` — on/off state and depth (Low/Medium/High) survive page reload, matching the existing `autoTTS` persistence behaviour
- [x] AI loading placeholder changed from "Thinking…" to "Working…" — "Thinking…" was misleading for vision requests and for text requests with thinking mode OFF
- [x] `removeImageBtn` now uses an SVG × icon instead of a `✕` text character — the only remaining non-SVG button after the v1.8.0 redesign
- [x] Keyboard focus trap added to persona panel and profile dropdown — Tab/Shift-Tab now cycles within the open panel; keyboard users can no longer accidentally tab to elements behind the overlay
- [x] Version bumped to 1.9.1
- [x] Export as Markdown — "Export MD" option added to the profile dropdown; `exportChatAsMarkdown()` in `chat.js` produces a `.md` file with a metadata header (date, persona), then one H2 section per message (`## You — HH:MM:SS`, `## AI Assistant (Persona Name) — HH:MM:SS`); AI content inserted verbatim (already stored as Markdown); image attachment noted inline; same smart topic slug and timestamp filename pattern as JSON save; `_chatTopic()` extracted to module scope so both export functions share it
- [x] Version bumped to 1.9.2
- [x] VoiceBox TTS engine option — new `voicebox-proxy/` FastAPI service bridges the browser to a local Voicebox MCP server (`voicebox.speak`) so AI responses can be spoken through VoiceBox as an alternative to the browser's Web Speech API; `_speak()` runs the MCP initialize + tools/call handshake and parses Voicebox's SSE-wrapped JSON-RPC responses (`_parse_sse_events`); exposed same-origin via new Nginx `location = /voicebox/speak` (auth-gated, `voicebox_speak` rate limit zone at 10 req/min burst 3) proxying to `host.docker.internal:17493`; new `#ttsEngineSelect` toolbar dropdown (Browser / VoiceBox) persisted to `localStorage` as `ttsEngine`, mirroring the `autoTTS`/thinking-mode persistence pattern; `speakText()` in `speech.js` now routes to `speakTextViaBrowser()` or `speakTextViaVoicebox()` based on `currentTTSEngine`; markdown-cleanup logic extracted to `stripMarkdownForSpeech()` in `utils.js` (shared by both engines, now unit-tested) — while relocating it, fixed a latent ordering bug where the fenced-code-block regex could never match because backticks were stripped first; VoiceBox has no cancel/onend signal available to the page, so its per-message button reflects only the initial request's outcome, not playback progress; 20 new pytest tests for `voicebox-proxy`, 6 new Jest tests for `stripMarkdownForSpeech`; new `voicebox-proxy-lint-test` CI job and `voicebox-proxy` host entry added to the `nginx-config-check` job
- [x] Version bumped to 1.10.0
- [x] `.tts-engine-select` restyled to match the resting/hover states of `.voice-btn`/`.toolbar-icon-btn` (neutral white/`#e2e8f0` border by default, sky blue on hover/focus) instead of the permanently-blue "active" palette borrowed from `.thinking-depth-select` — that palette only reads correctly on a control that toggles into an "on" state, which this selector doesn't have
- [x] VoiceBox set as the default TTS engine (was Browser) — `currentTTSEngine` initial value in `config.js`, the `selected` option in `index.html`, and the `localStorage` fallback in `main.js` all changed to `'voicebox'`
- [x] Version bumped to 1.10.1
- [x] VoiceBox generation caching + in-progress indicator — switched `voicebox-proxy` from the MCP transport to Voicebox's plain REST API (`POST /speak`, `GET /generate/{id}/status`, `GET /audio/{id}`), which is what actually exposes generation status and past-audio retrieval; MCP alone offered neither. `_speak()` now checks an in-memory `{sha256(profile+text): generation_id}` cache (`_generation_cache`, capped at 200 entries, oldest evicted) before contacting Voicebox at all; on a miss it starts generation then streams `/generate/{id}/status` to a terminal state before returning — Voicebox auto-plays the finished clip through the host speakers as a side effect of that flow, with no way to opt out. New `GET /voicebox/audio/{id}` (proxying Voicebox's own `/audio/{id}`) lets the browser fetch and replay a cache hit itself, since Voicebox does *not* auto-play for a lookup of a past generation. New Nginx `location ^~ /voicebox/audio/` (GET-only, auth-gated, same `voicebox_speak` rate zone); `/voicebox/speak`'s `proxy_read_timeout` raised to 90s to cover the now-blocking generation wait (backend's own httpx timeout raised 10s → 60s to match). Frontend: `speakTextViaVoicebox()`'s pending fetch doubles as the "generating" indicator — new `SPINNER_ICON` (chat.js) with a `.generating` CSS spin animation shown on the speak button (and `#speakerBtn`, for auto-TTS with no button) for the full synthesis wait; on response, a cache hit is played via a new shared `<audio>` element (`playVoiceboxAudio()`, lazily created) fetching the proxied audio URL — this *does* support a real stop control via `stopSpeaking()`, unlike a fresh (host-speaker) generation. While touching `stopSpeaking()`, fixed a latent bug where it referenced the bare `speechSynthesis` global unconditionally — would have thrown on browsers without Web Speech API support the moment VoiceBox-only usage started calling it unconditionally too. `voicebox-proxy` test suite rewritten for the REST design: 31 pytest tests (cache key/eviction, status-stream parsing, `_speak` cache-hit/miss orchestration, both routes)
- [x] Version bumped to 1.11.0
- [x] LaTeX/math rendering via KaTeX — vendored `katex.min.js` v0.18.1, `katex-auto-render.min.js`, `katex.min.css`, and its font set (SRI-pinned like `marked.min.js`/`dompurify.min.js`); `renderMathIn(element)` (`chat.js`) wraps `renderMathInElement()` with delimiters `$$...$$`, `\[...\]`, `$...$`, `\(...\)`, and the AMS environments (`\begin{equation}` etc.); called once per message at each *terminal* render point in `api.js`/`chat.js` (not on every live-streamed token, to avoid re-typesetting the whole message on each chunk). `trust` left at KaTeX's default of `false` — disables `\href`/`\url`/`\includegraphics`/`\html*`, the only mechanisms by which untrusted LaTeX could otherwise make KaTeX emit attacker-chosen HTML. `style-src 'unsafe-inline'` added to the Nginx CSP (script-src untouched) — KaTeX positions glyphs via computed inline `style` attributes, with no CSS-class-only alternative; residual risk bounded by the rest of the policy (no external hosts anywhere). **Found and fixed a real bug along the way**: `marked.parse()` always runs before KaTeX sees the text, and CommonMark's backslash-escape rule silently strips a backslash before ASCII punctuation (`\(` → `(`, `\\` → `\`, `\{` → `{`) — destroying the `\(...\)`/`\[...\]` delimiters and matrix row separators before KaTeX could ever match them. New `protectLatexDelimiters()`/`restoreLatexBackslashes()` (`utils.js`) swap the backslash for a `U+E000` placeholder before `marked.parse()` and restore it after; new `renderMarkdownToHtml()` (`chat.js`) is the one function combining this with `marked.parse()` + `DOMPurify.sanitize()` — all ~10 `DOMPurify.sanitize(marked.parse(...))` call sites across `api.js`/`chat.js` now route through it. `.message-content .katex-display` gets `overflow-x: auto` in `style.css` (KaTeX sets `white-space: nowrap` on display blocks, mirroring how `.message-content pre` already handles wide code blocks). Verified with a real Playwright browser session against the running stack (not just unit tests) — inline/display/paren-delimited math, code-block exclusion, malformed-expression fallback, live user-typed input, zero console/CSP errors. 10 new Jest tests (`tests/js/latex-protect.test.js`); corrected two stale doc claims found along the way (`escapeHtml()` isn't actually used for user message content — `.textContent` is; the documented `npx jest --testPathPattern=...` flag was renamed to `--testPathPatterns` upstream)
- [x] Version bumped to 1.12.0
- [x] Document attachment (.txt/.md/.pdf) with Q&A — new toolbar button (`attachDocumentBtn`) attaches a file and asks questions about its content. `.txt`/`.md`/`.markdown` are read entirely client-side (`file.text()`) and never reach a backend; `.pdf` is extracted by a new `doc-extract/` FastAPI service (`pypdf`) — deliberately server-side rather than vendoring pdf.js client-side, since pdf.js v6+ dropped its classic-script build entirely (ES-modules only), which would have made it the first ES-module dependency in an otherwise classic-script codebase, whereas a new microservice matches the `voicebox-proxy` pattern already established this session. Ollama's chat API has no `images`-equivalent field for arbitrary text attachments, so extracted text is folded directly into the message's own `content` via `buildDocumentMessageContent()` (`utils.js`) — wrapped in a delimited `--- Attached file: X --- ... --- End of X ---` block followed by the question (or a default prompt if none was typed) — which is what's actually sent to Ollama *and* stored in `conversationHistory`, so follow-up questions work automatically via normal history resend with no special-casing. Showing that full block in the chat bubble would be a poor experience for a long document, so `addUserMessage()`/`renderConversationHistory()` instead call `parseDocumentMessageContent()` (the inverse, regex-with-backreference match) on that same stored content to recover `{ documentName, question }` for display — a filename chip (`_appendDocumentChip()`) plus just the question; nothing is stored twice. Extracted text truncated to `MAX_DOCUMENT_TEXT_CHARS` (20,000 chars, `truncateDocumentText()`) uniformly across all three file types before being folded in — independent of doc-extract's own much larger 200,000-char server-side sanity cap — since the whole conversation history including this text is resent to Ollama every turn. New Nginx `location = /doc-extract/extract` (auth-gated, `doc_extract` rate zone at 10 req/min burst 3, `client_max_body_size 15m`); new `doc-extract-lint-test` CI job and `doc-extract` host entry added to `nginx-config-check`; `env_file: .env` added to the service in `docker-compose.yml` so its `MAX_UPLOAD_BYTES`/`MAX_TEXT_CHARS` env vars are actually reachable (an oversight caught before it shipped — they'd have been silently unconfigurable otherwise). Verified with a real Playwright browser session covering all three file types, including a `.pdf` generated on the fly via Chromium's own print-to-PDF and a live end-to-end round trip where the model correctly answered a question using only text extracted from the attached PDF. 16 new pytest tests for `doc-extract` (mocked `pypdf.PdfReader` plus two genuinely-invalid-bytes tests against the real library), 16 new Jest tests (`tests/js/document-attach.test.js`)
- [x] Version bumped to 1.13.0
- [x] ChatGPT-style "+" attach menu — the separate `attachImageBtn`/`attachDocumentBtn` toolbar buttons are replaced by a single `#attachMenuBtn` trigger that opens a popup (`#attachMenuDropdown`) with two options, "Add photos" and "Add files", each triggering the same underlying hidden `<input type="file">` as before. `openAttachMenu()`/`closeAttachMenu()` (`main.js`) mirror `openProfileDropdown()`/`closeProfileDropdown()` exactly, including outside-click close, Escape-to-close, Tab focus trapping (`_trapFocus`), and closing on window resize — the only difference is the popup anchors with `bottom` instead of `top`, since the trigger lives in the bottom input toolbar and must open upward to stay on-screen. Because both attachment types now share one button, the old per-button `.active` highlight (`#attachImageBtn.active`/`#attachDocumentBtn.active`) is replaced by `_updateAttachMenuActiveState()`, called from `clearImagePreview()`/`clearDocumentPreview()` and both file-input `change` handlers, which sets `#attachMenuBtn.active` whenever either an image or a document is pending. **Found and fixed a real bug along the way**: `#documentInput` (the hidden native file picker for document attachment, added in the 1.13.0 work) was never added to the CSS `display: none` rule that hides `#imageInput`/`#openInput` — it had been rendering as a visible native file-picker control on the page since that release. Verified with a real Playwright browser session against the running stack: menu open/close via click, outside-click, and Escape; both menu items correctly trigger their file picker; active-state indicator verified through a full image-attach-then-remove round trip; zero console errors
- [x] Version bumped to 1.14.0
- [x] Explicit `num_ctx` on every Ollama request + document budget raised to 28,000 chars — new `OLLAMA_NUM_CTX` constant (`config.js`, default `16384`) must match the Ollama server's actual configured context length (`OLLAMA_CONTEXT_LENGTH` env var or Modelfile `PARAMETER num_ctx`). **Found and fixed a real bug along the way**: `_buildRequestBody()` (`api.js`) previously sent no `num_ctx` at all for text/thinking requests — silently relying on whatever Ollama's own server-side default happened to be — while multi-turn-vision follow-ups hardcoded a mismatched `num_ctx: 8192`. Both paths now send `options.num_ctx` explicitly via a new `numCtx` parameter on `_buildRequestBody` (defaults to 16384 for tests that omit it; the real call site in `streamOllamaResponse` always passes `OLLAMA_NUM_CTX`), so behavior no longer depends on an assumption about the server's configuration. `MAX_DOCUMENT_TEXT_CHARS` raised from 20,000 → 28,000 chars (`config.js`) — sized against the 16K context so a document leaves roughly half the budget free for the system prompt, conversation history, thinking budget (up to 4,096 tokens at "Medium"), and the response itself; the user's original ask was 51,200 chars (~50K), but at ~3.5–4 chars/token that alone would consume 78–89% of a 16K context, leaving Ollama's default truncate-from-the-front behavior likely to silently drop the system prompt or part of the document on any multi-turn or thinking-enabled conversation — a data-loss risk rather than a clean error, so a more conservative number was chosen after presenting the math. Verified live against the running stack via Playwright + request interception: confirmed `num_ctx: 16384` is now present on both plain-text and document-turn requests, a 25,000-char document (previously would have been truncated at the old 20,000 limit) now passes through untruncated, and a 30,000-char document is correctly truncated to exactly 28,000 with the truncation flag set; zero console errors. 2 new Jest tests covering the new `numCtx` parameter (default and custom-value cases) alongside updates to the existing options-shape assertions
- [x] Version bumped to 1.15.0
- [x] Fixed a stale code comment on `VISION_MODEL_NAME` in `config.js` — still read "same model handles text and vision", a leftover from the reverted `gemma4:26b` single-model experiment (`a3cd74f`, reverted in `c017c28`); `MODEL_NAME` (`gemma4:e4b`) and `VISION_MODEL_NAME` (`gemma3:4b`) are distinct models again because `gemma4:e4b` ships no vision encoder in its GGUF
- [x] Security: bumped `js-yaml` 3.15.0 → 3.15.2 to clear high-severity Dependabot alert #17 (GHSA / CVE-2026-59870 — quadratic CPU consumption in `!!omap` resolution); transitive dev-only dependency via `jest` → `babel-plugin-istanbul` → `@istanbuljs/load-nyc-config`, used only to read coverage config, never untrusted YAML; lockfile-only in-range patch bump (`^3.13.1`), `npm audit` now clean, Jest suite still 101/101
- [x] Version bumped to 1.15.1
- [x] Branching & release workflow documented + Dependabot retargeted — `.github/dependabot.yml` now sets `target-branch: dev` on both ecosystems so version-update PRs land on `dev`, keeping `master` moving only through release PRs. New "Branching & Release Workflow" section in `CLAUDE.md` codifies: all PRs target `dev`; a release is a single `dev` → `master` PR merged as a **merge commit** (never squash/rebase — squash orphaned `dev`'s history in #17 and forced a `git reset --hard`); fast-forward `dev` back up to `master` immediately after each release. Fixes the recurring post-release `dev`/`master` divergence.
- [x] Version bumped to 1.15.2
- [x] Personas consolidated 13 → 11 — `default`+`helpful` merged into a single `assistant` ("Assistant"); `auEnglishEditor` renamed to `englishEditor` ("English Editor (Australian)", still the default persona and still the silent/output-only variant on first load); `quickwrite` moved to a non-dropdown `englishEditorExplained` key selected via the new toggle below. `<option>` list re-sorted alphabetical by label. No migration/alias map needed — persona is not persisted in saved-chat JSON (`saveChat` stores only `role`/`content`/`timestamp`/`hasImage`).
- [x] "Explain changes" toggle in the persona panel — a checkbox (`#editorExplainToggle`) shown only when the English Editor persona is active; swaps `currentSystemPrompt` between `systemPrompts.englishEditor` (silent) and `systemPrompts.englishEditorExplained` (explains its edits). State persisted to `localStorage` key `editorExplainChanges` and restored on load (default unchecked). `updateSystemPrompt()` (`chat.js`) resolves the variant via `_resolveSystemPrompt()`; its revert-on-cancel branch maps both variants back to the single `englishEditor` option value.
- [x] Per-persona settings memory — switching persona restores that persona's last-used thinking on/off + depth and TTS engine. Two new pure helpers in `utils.js` (`readPersonaPref`/`writePersonaPref`, validated, malformed-JSON-safe, Jest-covered in `tests/js/persona-prefs.test.js`); `localStorage` key `personaPrefs` (`PERSONA_PREFS_KEY` in `config.js`). `main.js` extracts `_setThinkingUI()`/`_setTTSUI()` as the single source of truth for those controls' DOM state (shared by the toggle handlers, the global session restore, and per-persona restore) and adds `_snapshotPersonaSettings()`/`_applyPersonaSettings()`/`_persistPersonaPref()`. Global `localStorage` writes (`thinkingOn`/`thinkingDepth`/`ttsEngine`) are unchanged; the per-persona store is written alongside them and wins on load when present.
- [x] Version bumped to 1.17.0
- [x] English Editor: "Explain changes" checkbox replaced by a 3-mode selector + word-level tracked-changes diff view — `#editorExplainToggle` is gone; the persona panel now has `#editorModeSelect` ("Editor output": Clean / Show changes / Explain) inside `#editorModeRow`, same visibility rule (English Editor persona only, `_updateEditorModeVisibility()` in `main.js`). New mutable global `currentEditorMode` (`'clean'|'changes'|'explain'`) + `EDITOR_MODE_KEY` (`config.js`), persisted to `localStorage['editorMode']`; one-time migration from the old `localStorage['editorExplainChanges']` boolean via the pure `migrateEditorModeValue()` helper (`utils.js`, Jest-covered). `_resolveSystemPrompt('englishEditor')` (`chat.js`) now returns the explaining prompt iff `currentEditorMode === 'explain'`, the silent prompt otherwise. Vendored Google **diff-match-patch** (official uncompressed build, verbatim, SRI-pinned in `index.html`, ESLint-ignored, provides both the browser `diff_match_patch` global and a CommonJS export via its `this[...]` trick). New pure `diffWords(original, revised)` (`utils.js`) implements the diff-match-patch word-mode recipe (whitespace-boundary tokens → unique-char mapping → `diff_main`/`diff_cleanupSemantic` → token-run expansion), returning `{op:-1|0|1, text}` segments. `renderEditorReply()` + `_buildEditorViewSwitch()` (`chat.js`): every Clean/Show-changes editor reply renders with an Original/Changes/Clean `<select>` that flips the view with no re-send; the "changes" view is built entirely via `createElement`/`createTextNode`/`textContent` (`<del>`/`<ins>`) — never `innerHTML` — so it adds no sanitisation-boundary surface. `api.js` tags a completed non-thinking, non-attachment English-Editor reply with `editorExchange: true` + `editorView` and renders it via the editor path; the raw model output is still stored verbatim in `conversationHistory` (the diff is always derived). `saveChat`/`handleOpenFile` carry `editorExchange`/`editorView` through the JSON round trip. Copy/speak still act on the clean revised text. New `tests/js/editor-diff.test.js` (17 tests: `diffWords` insert/delete/replace/no-change/whitespace/empty/multi-word/punctuation + `migrateEditorModeValue`).
- [x] Version bumped to 1.18.0
- [x] Syntax highlighting for code blocks (highlight.js) — vendored `highlight.min.js` v11.11.1 ("common" ~40-language build) + `css/highlight.min.css` (atom-one-dark theme), both SRI-pinned, `binary` in `.gitattributes`, ESLint-ignored. New `highlightCodeIn(element)` in `chat.js`: walks `pre code` blocks, skips `language-mermaid`, skips already-done (`data-hljs-done`), runs `hljs.highlightElement()` then **re-runs the output through `DOMPurify.sanitize()`** (keeps the "every innerHTML boundary is sanitised" invariant even though hljs emits only class-only spans), `try/catch` leaves a failing block as plain text. `hljs.configure({ ignoreUnescapedHTML: true })` once at load. `.message-content .hljs` in `style.css` overrides the theme's `#282c34` to the existing `#1e293b` slate `<pre>` background. CSP unchanged (`script-src` untouched — highlight.js needs no inline/eval).
- [x] Mermaid diagram rendering — vendored `mermaid.min.js` **v10.9.3** UMD single-file bundle (~2.8 MB; v11 dropped the clean UMD), SRI-pinned, `binary`, ESLint-ignored, loaded last of the vendored scripts. `mermaid.initialize({ startOnLoad: false, securityLevel: 'strict', theme: 'dark' })` at load. New `async renderMermaidIn(element)` in `chat.js` (fire-and-forget, not awaited): replaces each `pre code.language-mermaid` with `<div class="mermaid-diagram">` holding `DOMPurify.sanitize(svg, { USE_PROFILES: { svg: true, svgFilters: true } })`; a parse failure keeps the raw `<pre>` visible with `.mermaid-error`. `.mermaid-diagram { overflow-x: auto }` mirrors `.katex-display`. **CSP not weakened** — `securityLevel: 'strict'` + static analysis shows the only `Function()` use in the bundle is a lodash global-object fallback that short-circuits on `self` in a browser; the live browser CSP gate (`https://localhost/` with a `mermaid` fenced block, zero `script-src`/`unsafe-eval` console violations) is **UNVERIFIED — pending user confirmation** since the automation session could not authenticate past `/auth/login`.
- [x] `enrichRenderedContent(element)` helper in `chat.js` (`renderMathIn` + `highlightCodeIn` + `renderMermaidIn`) — every prior `renderMathIn(...)` call site in `api.js` (4) and `chat.js` (4, including `renderEditorReply`'s 'clean' view) now calls this instead, so a new render site can't silently skip highlighting/diagrams. `renderMathIn` kept as-is. `chat.js` gains a Node compat `module.exports` (mirrors `utils.js`/`api.js`).
- [x] `tests/js/code-render.test.js` — 7 Jest tests (jsdom env): `renderMarkdownToHtml` keeps ` ```mermaid ` as `<pre><code class="language-mermaid">` and ` ```js ` as `language-js`; `highlightCodeIn` adds `hljs` + `hljs-*` spans + `data-hljs-done` to a `pre code`, leaves `language-mermaid` untouched, is idempotent, no-ops on codeless input; `renderMermaidIn` no-ops without the `mermaid` global. New devDependency `jest-environment-jsdom@^30` (jest's own official DOM env — jest 30 no longer bundles jsdom) for this one file's `@jest-environment jsdom` docblock; the other 16 test files stay on the fast `node` env. Full suite 275 tests green.
- [x] `CLAUDE.md` — new "Code Highlighting (highlight.js)" and "Diagrams (Mermaid)" sections; JS Module Architecture rows for both vendored files + `enrichRenderedContent`; Security Invariants entries (hljs output re-sanitised; Mermaid `strict` + SVG-profile sanitise; CSP unchanged); "Updating Vendored Libraries" entries with source URLs/versions. `README.md` — feature list, tech-stack table, supply-chain row, file tree.
- [x] Version bumped to 1.19.0
- [x] Technical Expert persona prompt — added a line instructing it to emit diagrams as Mermaid inside a fence tagged exactly ` ```mermaid ` (never a bare fence or other label), so `renderMermaidIn()` picks them up. `gemma4:e4b` was intermittently tagging sequence-diagram fences ` ```sequence ` / bare, which rendered as plain code.
- [x] Version bumped to 1.19.1
- [x] Piper TTS replaces browser Web Speech synthesis — new self-contained `piper-tts/` FastAPI microservice (`POST /piper/speak` → raw `audio/wav`; offline neural TTS via the `piper-tts` PyPI package + `onnxruntime`; `en_US-lessac-medium` ONNX voice model **not committed to git** — fetched and SHA256-verified at image build time via `ADD --checksum`, hashes hard-coded in the Dockerfile). The TTS-engine dropdown is now **Piper (default) / VoiceBox**; the "Browser" option and ALL Web Speech *synthesis* code (`speakTextViaBrowser`, `SpeechSynthesisUtterance`, `loadVoices`, `availableVoices`, `currentUtterance`, the "Microsoft Catherine" voice pick) are removed. **Voice input (`webkitSpeechRecognition`, mic button, silence detection) is completely untouched.** New `speakTextViaPiper()` in `speech.js`: `fetch` → `Blob` → shared `ttsAudio` `<audio>` element (renamed from `voiceboxAudio`; `getTtsAudio()`/`onTtsAudioFinished()`), with a real stop control (`stopSpeaking()` pauses playback + `URL.revokeObjectURL`s the tracked `_ttsObjectUrl`) and mic-pause-while-speaking (`_pauseRecognitionForSpeech()`/`_resumeRecognitionAfterSpeech()`); the pending fetch doubles as the "generating" spinner exactly like the VoiceBox path, which is otherwise unchanged. `currentTTSEngine` default `'voicebox'` → `'piper'`; new `PIPER_SPEAK_URL` (`config.js`). New pure `normalizeTtsEngine()` helper (`utils.js`, `_TTS_ENGINES = ['piper','voicebox']`) maps a stale/legacy value — notably the removed `'browser'` — to `'piper'`; `main.js` migrates old `localStorage['ttsEngine']` and old `personaPrefs` entries on load, and drops the `'speechSynthesis' in window` guard on the autoTTS restore (auto-speak now works via Piper regardless of browser support). **Base-image divergence:** `piper-tts/Dockerfile` uses digest-pinned `python:3.12-slim`, not Chainguard distroless — `onnxruntime` segfaults on `import` under the distroless runtime (missing `libstdc++`/`libgomp`, no package manager); still multi-stage, non-root uid 65532, read-only root FS. New Nginx `location = /piper/speak` (auth-gated, `piper_speak` rate zone at 10 req/min burst 3, `proxy_read_timeout 60s`, buffered) + `PIPER_UPSTREAM` template placeholder (default `piper-tts:8004`); `render-nginx-conf.sh` allowlist extended and `nginx.conf` regenerated (round-trip verified). New `piper-tts` service in `docker-compose.yml` (no `extra_hosts`; `HOME=/tmp` for onnxruntime; added to `web-server` `depends_on`). New `piper-tts-lint-test` CI job (copy of `doc-extract-lint-test`; synthesis mocked so the ~60 MB model is never downloaded) — CI is now **6** jobs; `piper-tts` host added to `nginx-config-check`'s `/etc/hosts` stubs and cloud-render env. 13 new pytest tests (`piper-tts/tests/test_main.py`, synthesis fully mocked); `tests/js/speech.test.js` reworked (browser-synth tests removed; `speakTextViaPiper` fetch→blob→play + `stopSpeaking` revoke tests added under a jsdom docblock; `normalizeTtsEngine` tests; `stripMarkdownForSpeech` tests kept) — full Jest suite 282 green. Verified the real Docker image end-to-end: `python:3.12-slim` build succeeds, checksums verify, `POST /piper/speak` returns a valid 121 KB WAV as non-root uid 65532. **Live compose-stack verification then caught a hang** (worked standalone, wedged under the `cpus`/`pids` limits): fixed by (1) making `POST /piper/speak` a **sync `def`** route so blocking ONNX inference runs in FastAPI's threadpool instead of stalling the event loop (which had been killing `/health` too); (2) pinning onnxruntime to a **1-thread pool** via `_capped_session_options()` (scoped monkeypatch of `onnxruntime.SessionOptions` around `PiperVoice.load()`) + `OMP_NUM_THREADS=1` & friends in the Dockerfile/compose — unbounded, it sized the intra-op pool to the host core count and deadlocked during pool creation under the container limits; (3) `pids: 30 → 128`, `memory: 512M → 768M` (peak RSS ≈ 220 MB), and eager model load at startup (`PIPER_EAGER_LOAD`, `conftest.py` sets it off) so a broken model fails the healthcheck at boot not mid-request. Re-verified in the running stack: WAV back in ~0.5 s, `/health` answers in 3 ms *during* a synth, concurrent requests don't wedge. 4 more pytest tests (17 total). **Browser end-to-end then caught a third bug**: the `<audio>` element rejected the Piper `blob:` URL (`MEDIA_ELEMENT_ERROR: Media load rejected by URL safety check`) because the CSP had no `media-src`, so `<audio src="blob:…">` fell through to `default-src 'self'` which forbids `blob:`. Fixed by adding `media-src 'self' blob:` to the CSP `add_header` in `deploy/nginx/nginx.conf.template` (regenerated `nginx.conf`, round-trip + `nginx -t` verified) — `blob:` permits only the page's own `URL.createObjectURL` blobs (no external load, no exfil), and VoiceBox's same-origin `/voicebox/audio/{id}` replay is covered by the `'self'` part. `script-src` untouched. CLAUDE.md + README CSP notes updated.
- [x] Version bumped to 1.20.0
- [x] Ollama model selector — a compact `<select>` (`#modelSelect`, `.model-select`) added as the first control in `.toolbar-left` (before `#micBtn`), so the current model is always visible before the mic icon. On load `main.js` does `fetch(OLLAMA_TAGS_URL)` → new pure `parseOllamaModels()` (`utils.js` — GET `/api/tags` response → de-duplicated, case-insensitively-sorted model-name array; `m.name` with `m.model` fallback; `[]` for any malformed input, never throws) → builds the `<option>`s. Initial model = stored `localStorage['ollamaModel']` if still installed, else `MODEL_NAME` if installed, else the first installed model. The `change` handler only sets `currentModel` + rewrites `localStorage` — **no conversation reset, no lock** (unlike the persona selector), and **not** part of `personaPrefs` (a global preference). Graceful failure when Ollama is unreachable: the selector holds one option (stored/default model), `currentModel` is kept, a `showToast('Could not load model list from Ollama')` fires, and the app stays fully usable — the chat error only surfaces on an actual send. New `config.js` consts `OLLAMA_TAGS_URL` / `OLLAMA_MODEL_KEY` and runtime global `currentModel` (defaults to `MODEL_NAME`, which is now only the fallback); `api.js` passes `currentModel` (not `MODEL_NAME`) as `_buildRequestBody`'s `modelName` — **vision turns still route to `VISION_MODEL_NAME` unconditionally**. New Nginx `location = /ollama/api/tags` (GET-only via `limit_except GET { deny all; }`, `auth_request`-gated, new `ollama_tags` rate zone 12 r/m burst 3, `proxy_read_timeout 30s`, default body cap + buffering, reuses the `${OLLAMA_UPSTREAM}` placeholder) added to `nginx.conf.template` and regenerated `nginx.conf` (round-trip + `nginx -t` on `nginx:1.27-alpine` verified) — `/api/chat` and `/api/tags` are now the only two Ollama paths the proxy allows; `/api/generate`/`/api/pull`/`/api/delete`/`/api/create`/`/api/copy`/`/api/push` stay denied. ESLint globals added (`OLLAMA_TAGS_URL`, `OLLAMA_MODEL_KEY`, `parseOllamaModels`, writable `currentModel`). Tests: new `tests/js/ollama-models.test.js` (8 `parseOllamaModels` cases — sort/dedup/`m.model` fallback/empty/missing-key/non-array/non-object/skip-malformed) + 2 new `routing.test.js` cases (custom `modelName` → `requestBody.model`; vision ignores it). Full Jest suite 292 green; `lint:js` / `lint:html` / `check:sri` clean. CLAUDE.md (Architecture block, JS Module Architecture rows, Security Invariants, Key Configuration, Nginx Proxy) + README (feature list, Ollama settings, proxy security row, architecture diagram) updated.
- [x] Version bumped to 1.21.0
- [x] Conversation navigator rail — a thin vertical strip of markers on the right edge of `.chat-messages`, one per completed user turn, evenly spaced and vertically centred. Hovering a marker widens all markers and shows a floating dark preview card (single-line ellipsised question title + ≤25-word snippet of that turn's AI reply, with a caret pointing at the marker); clicking a marker scrolls that turn to the top of the viewport (`scrollIntoView({ behavior: 'smooth', block: 'start' })`) and flashes the message bubble for 1 s. The marker for the turn nearest the top of the viewport is kept `--active` (single highlight state, IntersectionObserver-driven; a click just scrolls there and the observer reassigns). Markers are mouse-only (not focusable, no keyboard shortcuts); the rail settles per completed turn (debounced MutationObserver rebuild), never during streaming. New `src/aia/scripts/nav-rail.js` (loaded after `api.js`, before `main.js`) with pure helpers `navRailCollapseTitle()` / `navRailTruncateWords()` / `navRailBuildModel()` (Node-exported for Jest) plus browser-only `initNavRail()` / `setNavRailEnabled()`; the rail reads nothing from other modules' internals — it walks `.message.user-message` / `.message.ai-message` straight from the DOM. Toolbar toggle `#navRailToggle` (after `#thinkingDepthSelect`) turns the whole rail on/off, default on, persisted to `localStorage['navRailEnabled']` (`NAV_RAIL_KEY` + `currentNavRailEnabled` in `config.js`, wired in `main.js` mirroring the autoTTS pattern) — an explicitly temporary home pending a consolidated Settings page. Nothing rail-related is written to saved-chat JSON. Markers are a 28×14 px transparent hit target with the visible 2 px tick drawn by `::before` (`gap: 0` for continuous vertical coverage); `.chat-messages` gained a thin custom scrollbar (`scrollbar-gutter: stable` + 8 px `::-webkit-scrollbar`) so the rail clears it; `@media (max-width: 600px)` hides the rail and its toggle; the flash animation is wrapped in `prefers-reduced-motion: no-preference` with a static-outline fallback. Built by three parallel sub-agents on non-overlapping file scopes (logic+tests / markup+CSS / config+wiring). 22 new Jest tests (`tests/js/nav-rail.test.js`); full suite 314 green; `lint:js` / `lint:html` / `check:sri` clean.
- [x] Version bumped to 1.22.0
- [x] Consolidated Settings — a single Settings lightbox (opened from the profile dropdown, next to Sign out) replaces the scattered toolbar / persona-panel preference controls (`#modelSelect`, `#ttsEngineSelect`, `#autoTTSBtn`, `#thinkingModeBtn`, `#thinkingDepthSelect`, `#navRailToggle`, `#editorModeSelect`, the persona `<select>` and `#personaPanel` — all removed). Two-pane master/detail (left: Models / Voice / Reasoning / Interface / Personas; right: the selected category's fields) with **per-category Save + Cancel + Reset this section**, a footer **Reset all**, and a per-field ↺ shown only when a value differs from its default. Persona section: an "Active persona" picker (disabled mid-conversation, matching the old lock) plus per-persona overrides for thinking mode/depth and TTS engine (nullable = "inherit global") and, for English Editor, the editor output mode; the 11 system prompts stay fixed (read-only disclosure). Vision model is now user-selectable. Two non-interactive toolbar badges (`#modelBadge`, `#thinkingBadge`) show the active model + thinking state and open Settings on click. **Preferences persist server-side, per authenticated user**, in a new **`settings-service`** (FastAPI + stdlib `sqlite3`, WAL, on the `vpal-settings-data` named volume) reached at auth-gated, rate-limited `/settings` — `GET /settings` (resolved `{global, personas, defaults}`), `PUT /settings/global`, `PUT /settings/persona/{key}`, `POST /settings/reset {scope[,keys]}`. Identity comes from a new `X-Auth-User` header that `/auth/verify` emits and nginx forwards (`auth_request_set` → `proxy_set_header`, overwriting any client value). Defaults resolve `env → per-user global → per-user persona override`; the 8 `VPAL_DEFAULT_*` env vars set the house defaults (`.env.example`). Frontend: new `src/aia/scripts/settings.js` (lightbox + `resolveSetting`/`resolveThinkingMode`/`resolveTtsEngine`/`diffSettings`/`buildMigrationPayload` pure helpers, Node-exported for Jest), `main.js` hydrates `window.vpalSettings` on load and does a **one-time `localStorage` → server migration** (`ollamaModel`, `ttsEngine`, `autoTTS`, `thinkingOn/Depth`, `navRailEnabled`, `editorMode`, `personaPrefs`), falls back to `config.js` constants + a toast if the service is unreachable (dialog shows a Retry state). `settings-service/` mirrors `doc-extract/` (Chainguard distroless, uid 65532, `/health`, digest-pinned; port 8005) — its Dockerfile seeds `/data` with uid-65532 ownership so the named volume inherits it. New Nginx `location = /settings` + `location ^~ /settings/` (auth-gated, `settings` zone 30 r/min burst 10, GET/PUT/POST only) via `${SETTINGS_UPSTREAM}` template placeholder (9 placeholders now); `nginx.conf` regenerated. New `settings-service-lint-test` CI job — CI is now **7** jobs; `settings-service` added to `nginx-config-check` host stubs + cloud-render env. Tests: 61 pytest (`settings-service`, real SQLite in a tmp dir), 3 new auth tests (`X-Auth-User` header), 31 new Jest (`tests/js/settings.test.js`) — full JS suite 345, auth 67, settings-service 61, all green; `lint:js`/`lint:html`/`check:sri` clean; `nginx -t` OK. Live-verified: TOTP login → hydration + migration succeed, unauthenticated → 302, settings survive a `settings-service` container restart on the volume. Built by 4 parallel sub-agents on non-overlapping file scopes (backend / auth+infra / lightbox module / frontend integration). `localStorage` is no longer the source of truth for any preference; `readPersonaPref`/`writePersonaPref`/`normalizeTtsEngine`/`migrateEditorModeValue` (utils.js) are now unused by app code (kept, with coverage — cleanup candidate).
- [x] Version bumped to 1.23.0
- [x] Consolidated Settings dead-code cleanup — the follow-up #34 deferred. `utils.js`: removed `readPersonaPref` / `writePersonaPref` / `normalizeTtsEngine` / `migrateEditorModeValue` (no app callers since the settings lightbox landed — `settings.js` inlines its own migration parsing) plus the now-orphaned `_THINKING_DEPTHS` / `_TTS_ENGINES` consts, their `module.exports` entries, and their `eslint.config.js` `crossModuleGlobals` entries; `parseOllamaModels` and `PERSONA_PREFS_KEY` kept (both still used — `settings.js` / `main.js` migration). Deleted `tests/js/persona-prefs.test.js`; dropped the `migrateEditorModeValue` block + import from `editor-diff.test.js` and the `normalizeTtsEngine` block + import from `speech.test.js`. `style.css`: deleted rules for elements the lightbox removed — `.persona-panel*`, `.persona-explain-toggle*`, `.editor-mode-select`, `.toolbar-icon-btn.tts-on`, `.toolbar-icon-btn.thinking-on`, `.thinking-depth-select`, `.tts-engine-select`, `.model-select`, all `#navRailToggle*` (kept `.persona-toggle-btn.locked`, still toggled in `chat.js`; kept `.toolbar-icon-btn.generating`; kept `.nav-rail` in the 600px media query) and refreshed three comments that cross-referenced deleted selectors. `speech.js:167` comment updated — `currentTTSEngine` is hydrated from the settings service, not a `ttsEngineSelect` control. Full gate green: `lint:js` / `lint:html` / `check:sri` clean, Jest 188/188 (11 suites; −23 for the removed dead-code coverage).
- [x] Version bumped to 1.23.1
- [x] Toolbar badge SVG icons render — `#modelBadge` / `#thinkingBadge` inject an inline `<svg>` that carried only a `viewBox`, and there is no CSS `svg` rule scoped to the badges, so the browser used the 300×150 default and the badge's `overflow: hidden` clipped it to an invisible sliver. `settings.js`: the shared `_SETTINGS_ICON_HEAD` constant now sets `width="15" height="15"` + `style="flex:none"` on the `<svg>`; `_settingsBadgeText()` gives the text `<span>` inline `overflow:hidden;text-overflow:ellipsis;white-space:nowrap;min-width:0` so a long model name ellipsises instead of clipping (`text-overflow` on the flex badge can't reach a child). No `style.css` change (the dead-code-cleanup branch was editing it). Verified in a static harness + the live Docker stack.
- [x] Version bumped to 1.23.2
- [x] Deploy / reload quality-of-life — (1) `add_header Cache-Control "no-cache" always;` at the nginx `server {}` level (not `location /`, which would drop the inherited security headers): with nginx's ETag/Last-Modified the browser gets a cheap 304 for unchanged `src/aia/` assets and a fresh 200 the moment a file changes, so a code change is picked up on a normal reload — no Ctrl+Shift+R. (2) `location = /piper/speak` gained `proxy_buffer_size 16k` + `proxy_buffers 128 16k` (2 MB) so a whole WAV stays in memory instead of spilling to a temp file (`an upstream response is buffered to a temporary file` gone from the error log). (3) `_settingsEnsureModelNames()` in `settings.js` fetches the model list with `redirect: 'manual'` and reloads on an `opaqueredirect` (expired session → nginx 302 → `/auth/login`) instead of showing a misleading "Could not load model list from Ollama" toast — mirrors `api.js`. (4) `cgr.dev/chainguard/nginx` pinned by digest in `docker-compose.yml` (was the one unpinned container). `nginx.conf` regenerated; drift guard + `nginx -t` green.
- [x] Version bumped to 1.23.3
- [x] Chat UX essentials — (1) **Regenerate** — a ↻ button in the last AI message's `.message-actions` (`regenerateLastResponse()` in `chat.js`): splices the trailing user+assistant pair out of `conversationHistory`, re-renders, and re-runs the send path (`streamOllamaResponse` re-pushes the user turn); re-passes the image for a vision turn; no-op mid-stream or when the last assistant isn't the final entry. (2) **Edit & resend** — a ✏️ button on the last user message (`editLastUserTurn()`): pops the trailing assistant + last user entry, loads the user text back into `#userInput` (just the question for a document turn, `content` for a plain turn), toasts "Attachment not carried over" for a document/image turn, focuses the composer. Only the *last* turn is editable/regenerable (rewinding a mid-conversation turn would invalidate everything after it — out of scope for v1). Buttons are surfaced by `_refreshTurnControls()` (end of `renderConversationHistory()` + three idempotent hooks in `api.js` after a stream settles); it bails when the DOM `.user-message` count ≠ history user-entry count (post-rollback desync guard). `sendMessage` / `sendMessageAndContinueListening` refactored to a shared `_dispatchSend()`. (3) **Keyboard shortcuts** (single `keydown` block in `main.js`): `Esc` — cancel an in-flight stream (`#stopBtn` visible via `offsetParent`) → else close an open panel → else focus `#userInput` (bails if the Settings lightbox is open); `Ctrl`/`Cmd`+`,` — open Settings (Models); `Ctrl`/`Cmd`+`Shift`+`O` — clear chat. New `.user-message .message-actions` right-aligns the edit button. Built by 2 parallel sub-agents (logic+tests / shortcuts+CSS+docs). New `tests/js/chat-ux.test.js` (22 tests — `_lastExchangeIndices`, `_editableTextFor`, `_refreshTurnControls` in jsdom, guard paths); full JS suite 210, `lint:js`/`lint:html`/`check:sri` clean. Harness-verified: regenerate re-streams with the right args, edit pops the exchange + loads the text + focuses, both buttons render at 15×15. CLAUDE.md + README updated.
- [x] Version bumped to 1.24.0
- [x] Conversation history (server-backed) + settings backup — an auto-saved, searchable list of past conversations, persisted **per authenticated user server-side**. New **`conversations-service/`** (FastAPI + stdlib SQLite/WAL, Chainguard distroless uid 65532, port 8006, `vpal-conversations-data` volume, Dockerfile `/data`-seed trick like `settings-service`): `GET /conversations?q=&limit=&offset=` (metadata list, newest-first), `GET /conversations/{id}` (full record + `body`), `PUT /conversations/{id}` (client-generated id, upsert, server timestamps, **100-per-user cap → evict oldest**, `body` > 1 MiB → 413), `DELETE /conversations/{id}` (idempotent), `GET /conversations/search?q=` (`LIKE` title + body), `GET /health`. Per-user via the `X-Auth-User` header nginx already forwards — **no auth-service change**. New nginx `location = /conversations` + `^~ /conversations/` (auth-gated, `conversations` zone 60 r/min burst 20, GET/PUT/DELETE, `client_max_body_size 2m`) via `${CONVERSATIONS_UPSTREAM}` placeholder (10 total); `nginx.conf` regenerated. New `conversations-service-lint-test` CI job — **8** jobs now. Frontend: new `src/aia/scripts/history.js` (History lightbox opened from a new profile-menu "History" item; search + a "search message text" toggle; conversation cards → click to load, ✕ to delete; footer count/cap + `navigator.storage.estimate()`; resilient error/Retry state) with pure helpers `chatTitleFrom` / `conversationRecordFrom` (base64 stripped, `hasImage` kept — same shape `saveChat` produces) / `filterConversations`. Auto-save: a `'c-…'` id is minted on the first turn; `PUT` after **every completed turn** (`hcTouchCurrent()` at the 3 `api.js` turn-complete points, debounced) and on `beforeunload` (`fetch` `keepalive` PUT — `sendBeacon` is POST-only). `clearChat()` → **"New chat"**: `await hcArchiveCurrent()` then wipe (no destructive confirm — nothing is lost); the profile-menu item + `Ctrl/Cmd+Shift+O` both route here. Loading a past chat leaves the active persona unchanged (card shows the persona it was created with). Settings lightbox gains a **Backup** row: Export → download `GET /settings` JSON; Import → upload JSON → `PUT`s → re-hydrate. Built by **4 parallel sub-agents** (backend / infra / lightbox+helpers / integration) against `docs/conversations-feature-spec.md`. Tests: 56 pytest (`conversations-service`), 15 new Jest (`tests/js/history.test.js`) — full JS suite 225, `lint:js`/`lint:html`/`check:sri` clean, `nginx -t` OK. Live-verified: unauthenticated → 302; API list/get/put/search/delete + per-user isolation (404) + 401 + 413 against real SQLite; 5 conversations survive a `conversations-service` restart on the volume; History lightbox renders cards, search filters, load swaps the conversation, auto-save PUTs. **Lead integration fixes:** Agent B's `nginx.conf` had Git-Bash path mangling (`C:/Program Files/Git/etc/ssl/...`) — regenerated with explicit `SSL_*` env; local `deploy/certs/` cert/key had drifted out of sync + stray `.pem;C` dirs (collateral from an agent's nginx-test container) — regenerated with mkcert (gitignored, not in the diff); added minimal `.settings-backup` CSS. CLAUDE.md + README updated.
- [x] Version bumped to 1.25.0
- [x] Conversation history — fix two defects found in post-merge review of #41 before the `dev` → `master` release. (1) **Blocker: auto-save was completely broken.** `conversationRecordFrom()` (`history.js`) embedded `id` in the returned record object, which `_hcPut()` sends as the PUT body; the service rejects any body key outside `{title, persona_key, message_count, body}` with `422 {"ok":false,"error":"unknown key: id"}`, so every per-turn auto-save, `hcArchiveCurrent()` and `hcBeaconSave()` PUT silently failed and nothing was ever persisted. CI missed it because `history.test.js` asserted `rec.id` *is* present while `test_main.py`'s PUT helper never sends `id` and `TestUnknownKeys` proves an extra key is rejected — the two suites enforced opposite contracts and nothing PUT a real `conversationRecordFrom()` output to the real service. Fix: `id` is no longer in the returned object (it already travels in the URL path); `history.test.js` shape test now asserts `rec` has *exactly* `{body, message_count, persona_key, title}` and no `id`. (2) **Medium: oversized / large bodies returned a bare nginx 500.** The `auth_request` sub-request re-checks the client body against `client_max_body_size` in the `location = /auth/verify` context, which inherited the server-level `1m`; any auth-gated request with a body over 1m (`/ollama/api/chat` 20m, `/doc-extract` 15m, `/conversations` 2m) failed the sub-request with 413, which nginx surfaces as `500` ("auth request unexpected status: 413") — and it capped `/conversations` at 1m regardless of its declared `2m`, colliding exactly with the service's own 1 MiB body cap so the service's JSON 413 was unreachable. Fix: `client_max_body_size 20m` on `location = /auth/verify` (no body is forwarded upstream — `proxy_pass_request_body off` — so this only affects the size check); `client_body_buffer_size 256k` on both `/conversations` blocks so a normal auto-save PUT is held in memory instead of spilling to a temp file every turn. `nginx.conf.template` edited + `nginx.conf` regenerated (drift guard + `nginx -t` verified). Live-verified against the running stack: a `conversationRecordFrom()`-shaped PUT now returns `200`, a >1 MiB body now returns the service's JSON `413` (not a 500), auto-save fires on every turn.
- [x] Version bumped to 1.25.1
- [x] ChatGPT-style UI overhaul — replaced the centered 900px card with a **full-viewport two-column shell** (`.app`, `display: flex; flex-direction: row; height: 100dvh`; `index.html` rewritten from `<body>` down). **Left `#appSidebar` (dark navy):** `#newChatBtn`, `#sidebarCollapseBtn`, the `#historySearch` row, the now-**inline** conversation-history list (`#historyList` / `#historyEmpty` / `#historyFooter`), and a **locked** `#accountTrigger` user block at the bottom. **Right `#appMain` (light):** a slim light `.app-main-header` (`#sidebarToggleBtn` hamburger, `.header-title` moved verbatim from the old dark `.chat-header`, `#chatOverflowBtn` "⋯"), then `#chatMessages` / hidden file inputs / `.chat-input` / `#navRail` (the nav-rail now anchors to `#appMain`, `position: relative`; `nav-rail.js` unchanged). The old top-right profile dropdown is **removed** — `.chat-header` / `.header-actions` / `.profile-menu` / `#profileTrigger` / `#profileDropdown` / `#historyMenuItem` / `#historyRoot` and all their CSS gone; its items redistributed: New chat → sidebar, History → the sidebar list itself, **Settings + Sign out → new bottom-left `#accountMenu` popup** (anchored to `#accountTrigger`, opens upward; `#logoutForm` moved here), **Save / Export MD / Open / Close → new `#chatOverflowMenu` popup** (anchored to `#chatOverflowBtn`, opens downward). **history.js:** the modal shell (`openHistory`/`closeHistory`/`_hcOnKeydown`/`_hcFocusable`/backdrop/`#historyLightbox`) deleted; `initHistory()` now just binds the static shell + fetches; new `hcNewChat()` (→ `clearChat()` + `closeSidebarDrawer()`), `hcRefreshSidebar()` (debounced ~800ms — re-renders the list after each turn / load / delete / New chat so a new conversation appears and its title updates, no reload), pure `resolveActiveCardId(list, currentId)` (drives the `.history-card.is-active` highlight; unit-tested); `_hcDoTouch` / `hcLoadConversation` / `_hcDeleteCard` / `chat.js clearChat()` all call `hcRefreshSidebar()`. **main.js:** `#accountMenu` + `#chatOverflowMenu` open/close (local closures, `getBoundingClientRect` + `position:fixed`, mirroring the attach menu), sidebar collapse/drawer — `toggleSidebar()` + hoisted `closeSidebarDrawer()` (window global for history.js), `matchMedia('(max-width: 768px)')` split, desktop collapse persisted to `localStorage['sidebarCollapsed']` (`SIDEBAR_STATE_KEY`, config.js — pure client view state, never synced), `#sidebarToggleBtn` shows while collapsed on desktop too; new `Ctrl/Cmd+B` shortcut; Escape chain extended (stream → drawer → account/overflow/attach menu → composer); Tab trap covers the two new menus; `resize` closes them. **style.css:** new `:root` **design-token layer** (`--color-*` / `--space-*` / `--radius-*` / `--shadow-*` / `--focus-ring` / `--sidebar-width` / `--z-*`); new shared **`.lightbox__backdrop` / `.lightbox__panel`** (+ `lightboxFadeIn`/`lightboxPopIn`) backing the Settings modal, and **`.menu-popup`** backing every JS-anchored popup — `.profile-dropdown` + `.attach-menu-dropdown` + their duplicated `@keyframes` deleted; the whole History-modal CSS block deleted; the Settings block adopts the shared shell (`settings.js` adds the classes in `initSettings()`) so it visually matches the new design; new breakpoint `@media (max-width: 768px)` for the shell (the existing 600px one — nav-rail hide + Settings full-screen — untouched). `eslint.config.js` globals updated (`hcNewChat` / `hcRefreshSidebar` / `resolveActiveCardId` / `closeSidebarDrawer` / `SIDEBAR_STATE_KEY` in; `openHistory` / `closeHistory` out). Full gate green: `lint:js` / `lint:html` / `check:sri` clean, Jest **230** (13 suites, +5 `resolveActiveCardId`). Live-verified against the running stack (authenticated browser): sidebar list renders + search + load + active-card highlight + refresh-after-turn (nginx `PUT /conversations → 200`); account menu → Settings / Sign out; `⋯` → Save / Export / Open / Close; Settings lightbox shares the token shell; desktop collapse persists across reload (`Ctrl/Cmd+B`); nav-rail still anchored to the messages column with working hover-card; mobile drawer end-state (`translateX(0)`, on-screen) confirmed. CLAUDE.md (new "App Shell / Layout" section + `history.js` / `main.js` / `chat.js` / nav-rail / Settings / Conversation History / Keyboard Shortcuts) + README updated.
- [x] Persona picker + persona-on-load (follow-up to the ChatGPT layout). (1) **The ▾ next to "AI Assistant" is now a lightweight persona picker** — `#personaMenu` (a `.menu-popup` built in `main.js` from `personaLabels` × `_PERSONA_KEYS`, one `menuitemradio` per persona, the active one checked), picking one calls a new exported `setActivePersona(personaKey)` (`settings.js`) that validates the key, no-ops when unchanged, else `PUT /settings/global {active_persona}` → merge → `applyResolvedSettings()`. It **no longer opens the Settings dialog** — per-persona detail (thinking/TTS overrides, editor mode, system-prompt disclosure) stays in Settings → Personas. Locked mid-conversation (`conversationHistory.length > 0` → toast, no menu). Wired into the Escape chain / Tab trap / outside-click / resize alongside the account + overflow menus. (2) **`hcLoadConversation()` (`history.js`) restores the loaded conversation's persona** — `await setActivePersona(data.persona_key)` before `renderConversationHistory()`, so follow-up turns continue under the persona the thread was created with; a new chat keeps the currently-active persona; a conversation with no stored `persona_key` (older saves) is left untouched. CSS: `#personaMenu button.is-active` leading-check style. `chat.js` `updateSystemPromptState()` toggle title updated ("Choose a persona" / "Start a new chat to switch persona"). Tests: `settings.test.js` +2 `setActivePersona` guard cases (invalid key / already-active → no fetch); full JS suite **232**, `lint:js`/`lint:html`/`check:sri` clean. Live-verified: picker switches persona with no dialog + no reload; loading a chat flips the header label to its stored persona; locked toggle shows the toast.
- [x] Version bumped to 1.26.0
- [x] Thinking mode — fix "the whole reply lands in the thinking box" on qwen3 (and any non-gemma thinking model). Root cause: `_buildRequestBody()` (`api.js`) appended a `{ role: 'assistant', content: '<|think|>  ' }` **prefill** whenever thinking was on — a gemma-only nudge from v1.5.5 (July, before Ollama's `think` param existed for gemma). Live-checked against the running Ollama: gemma4:e4b, qwen3.5:9b, qwen3:4b and deepseek-r1:8b **all** do clean native separation with `think: true` alone (`message.thinking` = reasoning, `message.content` = answer) — but **with** the prefill, qwen3.x returns everything as one garbled `message.content` blob with no `<|/think|>` marker, so `splitThinkingContent()` (which only knew gemma's `<|/think|>` token) returned `{ thinking: <everything>, answer: '' }`. Fix: **remove the assistant prefill entirely** (`api.js` — `thinkingToken` const + the `messages.push`); Ollama's `think` boolean + `options.thinking_budget` are the only thinking controls now. Plus defence-in-depth in `splitThinkingContent()` (`utils.js`): a non-empty `thinkingBuffer` is now trusted unconditionally (native mode — `message.content` is the answer even if it contains the literal text `"</think>"`); the inline fallback recognises `</think>` / `</thinking>` (qwen3 / deepseek-r1 style) in addition to `<|/think|>` and strips a leading `<think>` / `<|think|>` open tag. Tests: `routing.test.js` "thinking prefill" block replaced with "never injects an assistant prefill" (+ a history-preservation assertion); `thinking.test.js` +6 inline-`<think>` cases. Full JS suite 238, `lint:js` clean. Live-verified in the browser (qwen3.5:9b, thinking On·Low, Assistant persona): reasoning renders in the collapsed `<details>` block, the answer ("69" + working) renders below it as normal markdown.
- [x] Version bumped to 1.26.1
- [x] Thinking mode — "stuck in thinking" with `qwen3.5:9b` (and any large/heavy reasoner). Not a hang: verified by replaying the app's exact `/api/chat` body against Ollama — the model always terminates (`done_reason: "stop"`), it just emits 1,300–1,850 reasoning tokens even for trivial prompts (~50–75 s at ~25 tok/s on this box), preceded by up to ~40 s of silent cold model-load. Two real defects found: (1) **`options.thinking_budget` (Low=1024 / Medium=4096) is not an Ollama parameter** — it's a Gemini/Anthropic concept Ollama silently drops, and `think:"low"|"high"` string levels are ignored too (verified byte-identical output at a fixed seed on qwen3.5), so the Low/Medium/High control did *nothing*. (2) **Gemma sampling (`temperature 1.0 / top_p 0.95 / top_k 64`) was forced onto every model** — qwen3's Modelfile wants `top_k 20` and runs hot at 64. Fix (`api.js` `_buildRequestBody`): `thinking_budget` → `options.num_predict` (Low 4096 / Medium 8192, High uncapped) as a generous **total-token safety ceiling** — normal replies never reach it, a runaway reasoner is force-stopped in minutes instead of grinding to `num_ctx`; the Gemma sampling block is now gated on `/^gemma/i.test(modelName)`, every other model keeps its own Modelfile defaults + only `num_ctx`. UX (`api.js` streaming path + `style.css`): the "Thinking…" summary shows a live `12s · ~340 tokens` meta; a "Loading model… (first reply from a large model can take 30–60 s)" note replaces the static "Working…" after 4 s of no tokens; hitting the `num_predict` ceiling (`done_reason === 'length'`) is labelled ("Reasoning-effort limit reached…") instead of showing an empty bubble. Tests: `routing.test.js` option assertions rewritten + 3 non-Gemma cases; full JS suite **241**, `lint:js` / `lint:html` / `check:sri` clean. Live-verified against the running Ollama: `qwen3.5:9b` + `think:true` + `num_predict:4096` → `done_reason: stop`, thinking + answer both present.
- [x] Version bumped to 1.26.2
- [x] Mermaid CSP gate — **verified live** (was the one outstanding "pending user confirmation" item from the v1.19.0 Mermaid work). Checked in an authenticated browser at `https://localhost/` against the real response CSP header (`script-src 'self'` — confirmed **no `unsafe-eval`, no `unsafe-inline`**): rendered flowchart / sequence / subgraph+`classDef` diagrams and a deliberately-broken one through the real `enrichRenderedContent()` → `renderMermaidIn()` path, with a `securitypolicyviolation` event listener attached. Result: **zero CSP violations**, valid SVG each time, `securityLevel: 'strict'` confirmed (`performance` resource list shows **no external fetches**), broken diagram falls back to the raw `<pre>` + `.mermaid-error` as designed. Mermaid's injected `<style>` element + inline `style` attrs are covered by the existing KaTeX `style-src 'unsafe-inline'` exception; `script-src` is untouched. No code change — documentation only (`TODO.md`, `docs/session-status.md`). The forward-looking "re-run this gate after any Mermaid bump" note in `CLAUDE.md` stays.
- [x] `num_ctx` per-model adaptation — the `num_ctx` sent to Ollama is no longer a flat `OLLAMA_NUM_CTX` (16384) on every request. Recent Ollama builds expose `details.context_length` per model in `GET /api/tags` (verified against the running stack: `qwen3:4b`/`qwen3.5:9b` → 262144, `deepseek-r1:8b` → 131072; the gemma line reports none). New pure helpers in `utils.js` (Node-exported, Jest-covered): `parseModelContextLengths(json)` → `{ modelName: contextLength }` (positive-int only, `{}` for malformed) and `resolveNumCtx(modelName, ceiling, contextMap)` → `min(ceiling, model window)` when the window is known and smaller, else `ceiling`. **Cap-only by design** — never raised above `OLLAMA_NUM_CTX` even for a 128K model, since a larger KV cache is a host memory-budget decision, not something to auto-enable. New `config.js` globals `currentNumCtx` (what `api.js` sends) + `modelContextLengths` (the map). `settings.js` `recomputeNumCtx()` is called from `applyResolvedSettings()` (model may have changed) and after every `/api/tags` fetch; `main.js` does one `/api/tags` fetch at load so the cap applies on the first turn; `_settingsEnsureModelNames()` refreshes the map when the Settings dialog opens. `_buildRequestBody()` unchanged (already honoured a passed `numCtx`); call site passes `currentNumCtx`. **No behaviour change for the currently-installed models** (all report ≥128K or none → all resolve to 16384) — this is a defensive fix for installing a genuinely small-context model later. `MAX_DOCUMENT_TEXT_CHARS` still sized against the 16384 ceiling (documented). Tests: `ollama-models.test.js` +11 (`parseModelContextLengths` 5, `resolveNumCtx` 6); full JS suite **252**, `lint:js`/`lint:html`/`check:sri` clean. CLAUDE.md + README updated.
- [x] Version bumped to 1.27.0
- [x] Style consistency pass — scrollbars + Settings footer. (1) **Scrollbars unified**: only `.chat-messages` had a styled bar; every other scroll container (Settings panel/nav, the dark-navy `#historyList`, the `#1e293b` code blocks, `.thinking-content`, `.katex-display`, `.mermaid-diagram`) fell back to the ~16px native bar. New `--scrollbar-thumb*` tokens + one shared "Scrollbars" section in `style.css`: thin, transparent-track, 4px thumb — grey on light surfaces, translucent-white on the dark ones. Old ad-hoc `.chat-messages` block folded in (only `scrollbar-gutter: stable` stays inline). (2) **Settings "Export settings" / "Import settings"** were `.settings-section-reset` (tiny underlined text links) next to `#settingsResetAll` (a bordered pill). New `.settings-backup-btn` = same box metrics as `#settingsResetAll` (7px 14px / 8px radius / 12px / 1px border) as a neutral pill; `settings.js` swaps the two buttons' class. `#settingsResetAll` + footer/backup rules tokenised (no visual change). (3) `.settings-prompt-view` gets `max-height: 220px; overflow-y: auto` so a long system prompt scrolls in its box. No JS behaviour change; JS suite **252**, `lint:js`/`lint:html`/`check:sri` clean. Verified via computed-style + screenshot harness (session expired → couldn't check the live authed app).
- [x] Version bumped to 1.27.1
- [x] Full colour tokenisation of `style.css` (groundwork for theming — **no visual change**). Every hardcoded colour (~175 hex + ~44 `rgba()`) now resolves from a semantic `var(--…)`; the `:root` token set is expanded (surfaces, text, bubbles, code, controls, feedback, sidebar, panel-dark) and base hues are stored as `r, g, b` triples (`--color-accent-rgb` etc.) so tinted `rgba()` values track one source. Stale hex-referencing comments updated. **Verification:** `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' style.css` matches only inside the `:root` block; a computed-style harness diff (real `style.css` vs `git show HEAD:…`, ~130 colour properties across every surface) is identical except **one** sub-perceptual delta — the Settings text-input colour folds `#2d3748` → `--color-text` (`#1e293b`), matching the dialog's label text. `lint:html`/`check:sri` clean, JS suite 252. Sets up the ChatGPT monochrome repalette + dark mode (next PR).
- [x] Version bumped to 1.27.2
- [x] ChatGPT-style re-skin + Light / Dark / System themes (v1.28.0). Full monochrome repalette of the token values (drop the sky-blue accent; grayscale surfaces, black/white primary buttons, muted-blue links only) + a `data-theme` theme system: bare `:root` = ChatGPT-light; `@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) }` + `:root[data-theme="dark"]` = dark (two identical override lists). New `theme-boot.js` (first script in `<head>`, before `style.css` — CSP forbids inline) stamps `data-theme` from `localStorage['vpalTheme']` before first paint (no light-flash). New per-user `theme` global key in the settings-service (`system｜light｜dark`, `_THEMES` enum, `VPAL_DEFAULT_THEME` default `system`), edited in Settings → Interface, applied by `settings.js` `_applyTheme()` from `applyResolvedSettings()` — sets/clears `<html data-theme>`, mirrors to `localStorage`, wraps the swap in `.theme-switching` (`transition:none`) + a forced reflow so a `transition`ed `background: var(--token)` actually re-evaluates on the toggle (a Chromium quirk that otherwise left the send button the wrong colour — found via a computed-style harness). Vendored **Inter** (variable woff2, latin subset, ~100 KB, via `npm pack @fontsource-variable/inter`) → `src/aia/css/fonts/`, `@font-face` in `style.css`, `body` stack `'Inter', ui-sans-serif, …`; mono → system `ui-monospace` stack. Code blocks stay dark (`--code-bg: #0d0d0d`) in both themes. Message bubbles kept (just recoloured). `auth/static/login.css` re-skinned monochrome + a `@media (prefers-color-scheme: dark)` block (system-pref + system-font only — loads before the app, no Inter in the auth image). `<meta name="color-scheme" content="light dark">` on both pages. Tests: `settings.test.js` +5 (`_applyTheme` attr/localStorage/system-clear + `theme` resolution); `settings-service` +5 (theme in defaults, 3 valid choices, 2 × 422) → **66**; JS suite **257**. Docs: new **Theming** section in `CLAUDE.md` + README + `.env.example`. **CSP unchanged** — `theme-boot.js` and the `@font-face` `url()`s are all `'self'`.
- [x] Version bumped to 1.28.0
- [x] Settings save — surface the service's error string in the toast (v1.28.1). Was: any failed `PUT /settings/global｜/persona` showed a bare "Could not save settings". Now `_settingsSaveErrorToast(resp)` appends `resp.error` (e.g. `"unknown global setting: theme"`, `"theme must be one of: system, light, dark"`), so a rejected value — or a **settings-service still on a pre-upgrade image without a newly-added key** — is diagnosable. (This surfaced when the theme couldn't be saved: the running `vpal-settings` container predated v1.28.0's `theme` key and 422'd every save. Fixed operationally with `docker compose build settings-service && docker compose up -d settings-service`; this change makes the same class of failure obvious next time.) `_settingsSaveCategory` returns instead of throwing on `{ok:false}`; `_settingsSavePersonas` tasks throw with `resp.error` as the message. `lint:js` clean, settings tests 38.
- [x] Version bumped to 1.28.1
- [x] Personas — history grouping, remove Prompt Compressor, Australian English, per-persona SVG icons (v1.29.0). (1) **History grouped by persona** — `history.js` `_hcRenderList` now renders one **collapsible group per persona** (`groupConversationsByPersona()`, pure + Node-exported), most-recently-used persona first, a trailing "Unassigned" group for threads with no `persona_key`; a since-removed persona keeps its own group via `HISTORY_REMOVED_LABELS`. Collapsed keys persist in `localStorage[HISTORY_GROUPS_KEY]` (client-only, never synced); the active thread's group is force-expanded + scrolled into view. Per-card persona chip removed. (2) **Removed `claudePromptCompressor`** from `config.js` / `main.js` / `settings.js` / `history.js` / `settings-service/main.py`. Self-healing: `_settingsActivePersona` / `setActivePersona` already coerce/ignore an unknown key; `settings-service` `_resolved_global` now also coerces a stored `active_persona` for a removed persona → house default. (3) **Australian English** — shared `_AU_EN` clause appended to every selectable system prompt in `config.js`; the `legal` prompt's "in Australian" typo fixed to "in Australia". (4) **Per-persona SVG icons** — stored in the **settings-service** (`_PERSONA_ICONS` dict → read-only `personas.<key>.icon` in `GET /settings`, set after the stored-value merge so it can't be PUT/reset), mirrored by `config.js` `personaIcons` for the offline path. New `personaIconEl(key)` (`chat.js`) returns a `DOMPurify.sanitize`d `<svg>` (svg profile — same boundary as Mermaid). Rendered in `#personaMenu` (`icon · name · ✓`), Settings → Personas (identity row), the history group headings, and the chat header (`updateSystemPromptState` swaps `.header-bot-icon` per active persona). Tests: `history.test.js` +5 (`groupConversationsByPersona`, `_hcPersonaLabel`); `settings-service` +3 → **69** (icon present/read-only, no Prompt Compressor, removed-persona fallback); JS suite **262**. `lint:js`/`lint:html`/`check:sri` + `black`/`flake8` clean. `settings-service` image **rebuilt + recreated** so the `icon` field is served. Docs: CLAUDE.md + README.
- [x] Version bumped to 1.29.0

- **In-memory auth state** — brute-force lockout counters and TOTP replay protection are stored in process memory. Restarting `vpal-auth` clears this state, giving an attacker a fresh attempt window within the 90-second TOTP validity period. Accepted trade-off for a local 1–5-user deployment; documented in `README.md`. Mitigated by adding a persistent backing store (Redis, SQLite) if the threat model requires it.
- **VoiceBox has no cancel signal for a fresh generation** — a *fresh* synthesis is played by Voicebox itself through the host's speakers with no stop/onend callback back to this page, so the "stop speaking" toolbar button and per-message speak-button toggle can't interrupt it; it plays to completion on the host regardless. (A **cached** repeat *can* be stopped — it's replayed entirely by this page via a normal `<audio>` element.) Would need a `voicebox.stop`-equivalent endpoint to close for fresh generations too.
- **VoiceBox has no authentication of its own** — any client with network access to its local port can trigger speech or read past generations, so `/voicebox/speak` and `/voicebox/audio/{id}` rely entirely on VPAL's own session gate (`auth_request`) and Nginx rate limiting for protection, not on any credential Voicebox itself checks. Accepted trade-off for a local-only, loopback-bound deployment.
- **KaTeX requires `style-src 'unsafe-inline'`** — it positions glyphs (fraction bars, radicals, sub/superscripts) via computed inline `style` attributes on the spans it generates; there's no CSS-class-only way to do this, and it's inherent to how KaTeX (and MathJax's CommonHTML output) achieve pixel-perfect typesetting without native browser math layout support. `script-src` is untouched (no inline/eval anywhere); residual risk of the `style-src` exception is bounded by the rest of the CSP (`default-src 'self'`, `connect-src 'self'`, no external hosts in `img-src`), so an inline style still can't exfiltrate data or load an external resource.
- **TTS doesn't read LaTeX aloud in spoken-word form** — `stripMarkdownForSpeech()` strips LaTeX delimiters/markdown formatting for cleaner speech, but doesn't translate math into spoken English (e.g. `\frac{1}{2}` is read as literal characters, not "one half"). Both TTS engines (Browser and VoiceBox) are affected equally; translating LaTeX to natural speech is a materially harder problem than typesetting it and was out of scope here.
- **`protectLatexDelimiters()` only protects backslash + `( ) [ ] { } \`** — not the full CommonMark escapable-punctuation set (e.g. `\&`, used for column alignment in `align`/`matrix` environments, is not protected and will still lose its backslash to markdown). Narrower than a byte-perfect fix on purpose: broadening it risks changing the rendered output for genuine non-LaTeX uses of backslash-escaped punctuation in prose (e.g. someone deliberately writing `\%` for a literal `%`). Would need revisiting if multi-row matrix/align environments turn out to be common in practice.
- **Generation cache is exact-match only, in-memory, unbounded by time** — a cache hit requires byte-identical text and profile; no fuzzy/semantic matching. Cache is cleared on `voicebox-proxy` restart and capped at 200 entries (LRU eviction) with no TTL, so a stale entry can in principle outlive its underlying Voicebox history entry (rare in practice, and the failure mode is just a 404 on replay, not incorrect audio).
- **`num_ctx` adapts *down* per model, never *up*** — since v1.27.0 the `num_ctx` sent is `OLLAMA_NUM_CTX` (16384) reduced to the selected model's advertised `context_length` when that is smaller, but it is never raised above 16384 even for a 128K-context model: a larger KV cache is a host memory-budget decision for the operator, not something to enable automatically. Models whose `/api/tags` entry omits `context_length` (the gemma line, at time of writing) always use the full ceiling. `MAX_DOCUMENT_TEXT_CHARS` is still sized against the 16384 ceiling, so a genuinely small-context model + a large attached document can still overflow context. (The vision model *is* selectable — Settings → Models, since v1.23.0.)
- **Attached documents are truncated, not chunked/retrieved** — this is simple context-stuffing (the first `MAX_DOCUMENT_TEXT_CHARS` of extracted text, folded whole into one message), not a real RAG pipeline with embeddings/chunking/retrieval. A document longer than the truncation budget will have its tail silently unavailable to the model (a toast notes when truncation happens, but the model itself has no way to say "the answer might be further in the document — it was cut off"). Would need a proper retrieval step (chunk + embed + similarity search) to scale beyond a truncation budget.
- **Scanned/image-only and encrypted PDFs are rejected outright** — `doc-extract` has no OCR fallback (a scanned PDF parses successfully but yields zero extractable text, which is reported as an error rather than silently attaching nothing) and no password-prompt flow for encrypted PDFs. Accepted scope boundary; both would need a materially different extraction pipeline (Tesseract for OCR, a password-input UI for encrypted files).
- **Document text counts against context on every subsequent turn** — like images, once a document is attached to a message, its (possibly 28,000-character) extracted text becomes a permanent part of `conversationHistory` and is resent to Ollama on every following turn, not just the one where it was attached. `MAX_HISTORY_MESSAGES` eventually trims it once enough turns have passed, but a long document early in a long conversation makes every reply slower/more expensive in the meantime — the same trade-off the codebase already accepts for attached images.
- **Mermaid bundle is ~2.8 MB and pinned to the 10.9.x line** — the v10.9.3 UMD single-file bundle is large (loaded on every page view, though cached by the browser after the first) and Mermaid v11 dropped the clean UMD build this integration depends on. Accepted for a local-only app. A future move to v11 would need either a UMD/IIFE shim or a bundler step (the codebase currently has no build step), plus a fresh CSP gate.
- **Mermaid CSP gate not verified by automation** — `securityLevel: 'strict'` plus static analysis of the bundle (the only `Function()` constructor use is a lodash global-object fallback that short-circuits on `self` before evaluation) indicated Mermaid v10.9.3 runs cleanly under `script-src 'self'` with no `unsafe-eval` — and this was **confirmed by a live browser check on 2026-08-30** (authenticated `https://localhost/`, real CSP header, `securitypolicyviolation` listener across flowchart / sequence / subgraph / broken-diagram renders → zero violations, no external fetches). The gate is inherently a browser check, not a CI one; re-run it after any Mermaid version bump. If a violation ever appears, the fix is to pin back / go SVG-only server-side, **never** to weaken the CSP.

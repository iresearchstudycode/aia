# diff-match-patch.js is vendored verbatim and SRI-pinned in src/aia/index.html.
# Mark it binary so git never rewrites its line endings on any platform: the
# docker-compose bind mount serves the working-copy bytes straight to the
# browser, which verifies them against the integrity= hash. (The file is
# committed with LF endings; the hash is computed from those bytes.)
src/aia/scripts/diff-match-patch.js  binary
